const DashboardTitleComponent = ({ text }: { text: string }) => {
  return (
    <h2 className="text-[18px] sm:text-[22px] font-semibold mb-2">{text}</h2>
  );
};

export default DashboardTitleComponent;
